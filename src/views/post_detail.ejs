<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= post.title %></title>
    <style>
        body { font-family: sans-serif; margin: 2em; }
        .container { max-width: 800px; margin: auto; }
        .post-header { border-bottom: 1px solid #ddd; margin-bottom: 1em; padding-bottom: 1em; }
        .post-meta { color: #555; font-size: 0.9em; }
        .post-content { margin-top: 2em; }
    </style>
</head>
<body>
    <div class="container">
        <% if (post) { %>
            <div class="post-header">
                <h1><%= post.title %></h1>
                <div class="post-meta">
                    <span>작성자: <%= post.writer ? post.writer.name : 'N/A' %></span> |
                    <span>작성일: <%= new Date(post.createdAt).toLocaleString('ko-KR') %></span> |
                    <span>타입: <%= post.post_type %></span>
                </div>
            </div>
            <div class="post-content">
                <p><%- post.contents %></p>
                <% if (post.mp4_url) { %>
                    <video width="100%" controls>
                        <source src="<%= post.mp4_url %>" type="video/mp4">
                        Your browser does not support the video tag.
                    </video>
                <% } %>
            </div>
        <% } else { %>
            <h1>게시글을 찾을 수 없습니다.</h1>
        <% } %>
        <br>
        <a href="/posts">목록으로 돌아가기</a>
    </div>
</body>
</html>
